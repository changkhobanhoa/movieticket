import 'package:flutter/material.dart';

class InforUser extends StatelessWidget {
  const InforUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
