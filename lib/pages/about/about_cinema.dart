import 'package:flutter/material.dart';

class AboutCinema extends StatelessWidget {
const AboutCinema({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}